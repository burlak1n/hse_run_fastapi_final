{
  "id": null,
  "uid": "admin-overview",
  "title": "Admin Overview",
  "tags": ["prometheus", "admin"],
  "timezone": "browser",
  "schemaVersion": 30,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "timeseries",
      "title": "Admin HTTP Requests Total",
      "id": 1,
      "datasource": "Prometheus",
      "targets": [
        { "expr": "admin_http_requests_total", "legendFormat": "total" }
      ],
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Admin HTTP Exceptions Total",
      "id": 2,
      "datasource": "Prometheus",
      "targets": [
        { "expr": "admin_http_exceptions_total", "legendFormat": "total" }
      ],
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8}
    }
  ]
} 